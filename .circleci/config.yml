version: 2
workflows:
  version: 2
  test:
    jobs:
      - test-2.7
jobs:
  test-2.7: &test-template
    docker:
      - image: circleci/python:2.7-jessie
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: |
            sudo pip install --upgrade pytest pytest-mock pytest-cov
            sudo pip install -e .
      - run:
          name: Run tests
          command: make test
